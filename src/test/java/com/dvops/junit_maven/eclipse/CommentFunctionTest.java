package com.dvops.junit_maven.eclipse;

import static org.junit.jupiter.api.Assertions.*;

import java.util.List;

import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import com.dvops.maven.eclipse.Comment;

class CommentFunctionTest {

	@BeforeEach
	void setUp() throws Exception {
	}

	@AfterEach
	void tearDown() throws Exception {
	}

	@Test
	void testAddComment() {
		return;
		//fail("Not yet implemented");
		//List<Comment> testSc = getComment();
		//Assert that Comment Collection is equal to Comment collection size  4
		//assertEquals(testSc.size(), COMMENT_COLLECTION_SIZE);
		//Act
		//sc.addComment(s1);
		//Assert that Comment Collection is equals to the song collection size 4 + 1
		//assertEquals(sc.getComments().size(), COMMENT_COLLECTION_SIZE+1);
	}

}
